---
framework: mocha
preMerge:
  pattern:
    - "./test/test-python-shell.js"
preRun:
     command:
        - npm ci
        - npm ci --save-dev typescript && npx tsc --version && npx tsc -p ./
postMerge:
  pattern: 
    - "./test/test-python-shell.js"
postRun:
     command:
        - node --version
configFile: mocharc.yml
nodeVersion: 12.22.6
version: 1.0
